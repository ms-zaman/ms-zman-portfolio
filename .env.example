# Copy to .env for local dev; set the same keys in Netlify → Site settings → Environment variables.

# Google Analytics 4 measurement id (falls back to the site's own id when unset)
PUBLIC_GA_ID=

# Microsoft Clarity project id (clarity.microsoft.com → Settings → Overview).
# Leave empty to disable Clarity entirely.
PUBLIC_CLARITY_ID=
